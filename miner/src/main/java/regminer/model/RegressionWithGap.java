package regminer.model;

public class RegressionWithGap extends Regression{

    public RegressionWithGap(String bugId, String bfcId, String buggyId, String bicId, String workId, String testCase) {
        super(bugId, bfcId, buggyId, bicId, workId, testCase);
    }
}
